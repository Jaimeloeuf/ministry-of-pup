{
  "name": "mop-invoice",
  "description": "Lib to generate PDF invoices for MOP sales",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JJ",
  "license": "MIT",
  "peer-dependencies": {
    "base64-stream": "^1.0.0",
    "pdfkit": "^0.9.0"
  }
}
